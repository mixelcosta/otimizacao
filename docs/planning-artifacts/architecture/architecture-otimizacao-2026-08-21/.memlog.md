---
scope: Módulo de sugestão de upgrade com foco em custo-benefício (feature slice sobre o otimizacao existente)
purpose: Espinha de arquitetura para orientar épicos/histórias e desenvolvimento; resolver o bloqueador L4/L5 da Vitrine
altitude: feature (uma fatia sobre um sistema brownfield maduro)
updated: 2026-08-21T12:17
---

- (constraint) Investigacao brownfield: .NET 8, C# 12, TreatWarningsAsErrors, vertical-slice por projeto de feature (Features.Drivers, Features.Upgrade, Features.Licensing, Features.LifeCounter), sem MediatR/CQRS, DI manual via construtor com defaults (exceto WindowsService que usa Host.CreateDefaultBuilder). Fronteira Agent-UI via RoteadorIpc (named pipes, dispatch por string de Metodo). xUnit, fakes manuais (sem Moq/NSubstitute), sem builders compartilhados entre suites de teste. Nomenclatura em portugues, sufixos Servico*/Coletor*/Leitor*/Gerador*/Validador*/Repositorio*/Provedor*
- (constraint) BUG CRITICO DE BUILD encontrado: .gitignore linha 20 (regra 'data/') apaga do controle de versao Features.Upgrade/Data/hardware_catalog.json e Features.Drivers/Data/whql_catalog.json (EmbeddedResource referenciados nos .csproj). Build falha (CS1566) em clone novo. Precisa fix (ancorar regra do gitignore na raiz, ex. '/data/') antes de qualquer trabalho nesses projetos
- (constraint) Existem DUAS implementacoes divergentes de sugestao de upgrade: (1) Features.Upgrade -- ValidadorCompatibilidade + GeradorSugestoes + CalculadoraGargalo, testada, com validacao real de compatibilidade contra catalogo JSON; (2) HardwareOptimizer.App/ViewModels/UpgradeViewModel.cs -- catalogo hardcoded na propria ViewModel (switch de string de socket), sem validacao de compatibilidade, sem teste. A tela real que o usuario ve usa a (2), nao a (1). AgenteUpgrade (chatbot LLM) e a unica parte da UI que chama Features.Upgrade, via metodo IPC chat_upgrade/analise_upgrade
- (constraint) Tres fontes de dados de hardware ja existem ou sao pedidas, sem reconciliacao: BuildCores Open DB (GitHub, usado pelo AgenteUpgrade/chat), catalogo JSON estatico hand-curated ~15 pecas (usado por ValidadorCompatibilidade), e TechPowerUp (FR-19 do PRD, fonte nova pedida pelo usuario para o ganho estimado). RepositorioWhqlEstatico (Features.Drivers) tambem e catalogo estatico hardcoded (~10-20 drivers) com comentario no codigo dizendo 'MVP offline; producao usa REST API'
- (constraint) L4 confirmado no codigo: Inventario.cs so tem SaudeDiscos (dados S.M.A.R.T., sem capacidade/interface/slots livres). L5 confirmado: CategoriaAcao.Cpu e .Memoria existem no enum mas CatalogoPadrao.Criar() nunca gera AcaoOtimizacao com essas categorias. TipoPecaUpgrade (Cpu/Gpu/Ram/SsdM2/Fonte) e um enum SEPARADO para pecas de upgrade, ja existe
- (constraint) FR-4/FR-5 (Event Log BSOD/WHEA) sao greenfield total -- zero codigo de leitura do Windows Event Log existe. Os hits de 'WHEA'/'BSOD' no codigo sao de MedicaoEstresse (parsing de teste de estresse acionado pelo usuario), nao historico passivo
- (constraint) FR-6/FR-7 (aprovacao/rollback) ja tem base solida reaproveitavel: ExecutorControlado (exige PodeAplicar + PreCondicoes, reverte categoria inteira em regressao) + ServicoBackup (snapshot generico com checksum) + AtualizadorDrivers.ExportarBackupAsync/InstalarAsync (backup/instalacao driver-especifico via pnputil, exige elevacao runas)
- (constraint) BIOS (FR-2/FR-3) ja tem fluxo desenhado: ModuloBios + AnalisadorBios (so recomenda com ganho real e risco nunca menor que Medio) + BancoCuradoBios (hardcoded, 3 placas) + GeradorGuiaBios (guia passo a passo, nunca grava BIOS -- principio ja no codigo). Fonte de dados e o gap, igual driver/software
- (decision) Consolidacao: Features.Upgrade (testado, com validacao real) vira a base unica de sugestao de upgrade. UpgradeViewModel deixa de ter catalogo/logica propria hardcoded e passa a consumir Features.Upgrade. Elimina a divergencia entre as duas implementacoes existentes
- (decision) Fontes de dados de hardware reconciliadas por papel: TechPowerUp (FR-19) alimenta especificamente o ganho estimado/benchmark da Vitrine; catalogo JSON estatico continua servindo compatibilidade fisica (socket/RAM/PSU) ate ter fonte melhor; BuildCores Open DB fica isolado no chat do AgenteUpgrade, nunca vira fonte oficial de dado usado em FR do produto
- (decision) Paradigma ratificado: modular monolith vertical-slice por feature (Core/Agent/Features.*/Ipc/App), sem paradigma novo introduzido
- (decision) AD-1 a AD-7 travados na espinha: reuso de ExecutorControlado/ServicoBackup (AD-1), Features.Upgrade como fonte unica de sugestao (AD-2), papel fixo de cada fonte de dados TechPowerUp/catalogo estatico/BuildCores (AD-3), IProvedorFonteOficial com allowlist de dominios (AD-4), Event Log no Agent + correlacao no dominio (AD-5), Armazenamento como componente novo separado de SaudeDisco (AD-6), CategoriaAcao e TipoPecaUpgrade continuam catalogos separados (AD-7)
- (direction) Entregavel unico: a espinha (usuario dispensou deck/material de apresentacao)
- (event) Reconciliacao com PRD concluida: 5 gaps encontrados e corrigidos -- AD-8 (ordenacao FR-10 entre fatias), AD-9 (decisao explicita: L5 nao bloqueia Vitrine, catalogo cresce incremental), margem de confianca/data de atualizacao como campos de contrato (GanhoEstimado), NFR opt-in restatado em AD-4, Conformidade e coletor de Armazenamento adicionados ao Deferred
- (event) Lint deterministico limpo apos reordenar AD-5 a AD-9 para ordem ascendente. Iniciando Reviewer Gate: rubric walker + verificacao web de versoes + adversarial two-units-diverge
- (event) Reviewer Gate concluido (rubric + verificacao de versoes + adversarial). Fixes aplicados: AD-10 novo (fronteira de dados comerciais de Loja Parceira, finding critico), AD-3 estendido para cobrir GanhoEstimado da trilha gratis tambem, AD-4 esclarecido para um unico dono de IProvedorFonteOficial, AD-8 reescrito com dono unico + tipo Custo comparavel + shape do resultado composto, Capability Map corrigido, Deferred ganhou: ordenacao dentro da vitrine (PRD S10 item5), mecanismo comercial por loja, fonte do ganho da trilha gratis, riscos de atualidade (.NET 8 EOL nov/2026, xUnit em modo manutencao, acesso comercial TechPowerUp)
- (decision) Decisao com o participante: V1 usa a camada gratuita da TechPowerUp (itens em destaque/geracao atual); dataset comercial completo fica para depois se justificar investimento. AD-3 marcada [ADOPTED] nesse ponto
- (event) spine finalized
